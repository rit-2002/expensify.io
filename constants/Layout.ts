/**
 * Visible tab bar height excluding device safe-area inset.
 * Final rendered tab bar height = TAB_BAR_VISUAL_HEIGHT + insets.bottom
 */
export const TAB_BAR_VISUAL_HEIGHT = 60;

/** Distance from the visible bottom boundary (above the system nav bar) to the FAB center. */
export const FAB_CENTER_OFFSET_FROM_VISUAL_BOTTOM = 90;

/** Height of the header bar on form/modal screens (p-4 + text ~24px). */
export const FORM_HEADER_HEIGHT = 56;
